"""Application layer - core business logic for operator copilot.

Contains:
- Typed state definitions
- Abstract ports (interfaces)
- Graph definitions
- Use case orchestration

This layer has NO dependencies on frameworks or concrete implementations.
It depends only on ports, which are implemented by adapters.
"""
